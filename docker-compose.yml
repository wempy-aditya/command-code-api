services:
  redis:
    image: redis:7-alpine
    container_name: command-api-redis

    restart: unless-stopped

    ports:
      - "6378:6379"

    volumes:
      - redis_data:/data

volumes:
  redis_data:
